{
  "name": "timegrid",
  "version": "1.1.0",
  "description": "TimeGrid - collaborative time-schedule editor (PHP backend, PWA)",
  "scripts": {
    "start": "php -S localhost:8080 -t public",
    "dev": "php -S localhost:8080 -t public",
    "serve-static": "npx serve public"
  },
  "devDependencies": {
    "serve": "^14.2.1"
  }
}
